class Solution(object):
    def LongestSubstring(self, s):
        """
        :type s: str
        :rtype: int
        """
        res = set()
        for i in range(len(s)):
            if s[i] not in res:
                res.add(s[i])
            else:
                 


        return answer.next
